Pattern 

ChoicePattern
QuantityPattern alternatively and implicitly OptionalPattern when min = 0 and is valid

MatchKeywordToken 
MatchIdentifierToken
MatchNumericToken
MatchSymbolToken 
MatchStringToken 
MatchCharToken 

PatternSwitchParser

Valid patterns definitions:

Pattern: at least one PatternType in pattern_list array

ChoicePattern: at least one PatternType pattern_list array
QuantityPattern: must have a valid PatternType assigned to it and max >= min and min >= 0

Match[X]Token: they are primitives and always valid

PatternSwitchParser: always valid

Collapse operation preconditions:
 	param A: PatternType
 	param B: PatternType	
	
	cond1 = A == B	

	when A 	
	cond2 = 

Valid collapse operations:
