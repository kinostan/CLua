/* 
    Generator requirements:
    -Generates fully valid IR and then emits code from initial set of patterns 
    and definition of nodes.
    -Must allow generation of IR which expresses error recovery.

    -It must disambiguate patterns to determine which node to emit
*/