Objectives for tomorrow:
-Review current lexer/parser pipeline
    -Consider how ParserContext is going to interact with 
    NodeManager and if the current system could support 
    CLua and LuaU grammars in theory (write down implementation files to consume and emit ast trees primarly).
        
    -Review the error emittion to fit into the requirements of general purpose parser
-Improve that pipeline
    -Record emitted nodes as in the current architecture it's simple enough by tracking index 
    thanks to memory being incremented lineary without fragmentation, although in the future
    I will have to keep track of list of memory regions that have been allocated between record and
    set session.

Long term point of the current objectives:
    -Set up infrastructure to allow to be able to generate
    parser code for multiple languages which are like LuaU or CLua
    -This in turn allows to write one simple shared parser builder
    which based on that allows to emit multiple language parsers 
    within one multilanguage grammar expression tree.

Trade offs:
    -Loss of specialization (slightly worse performance potentiallyc)
    -Loss of explicitiness in NodeType 
    (It's no longer a unified enum and now it's type is an implicit by being an unsigned integer)